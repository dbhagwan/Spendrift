# Spendrift

An AI-first personal finance copilot for iOS and iPadOS. Spendrift syncs your bank accounts, reads your receipts, learns your spending behavior, forecasts where the month is heading, and answers one question every day: **what's safe to spend today?**

<p>
  <img src="Spendrift/Resources/Assets.xcassets/AppIcon.appiconset/icon-dark.png" width="96" alt="Spendrift icon" />
</p>

## What's in this repo

| Path | What it is |
|---|---|
| `Spendrift/` | SwiftUI app (iOS 18+ / iPadOS 18+, Swift 6) |
| `SpendriftWidgets/` | WidgetKit extension — Home Screen + Lock Screen widgets |
| `Backend/` | TypeScript backend scaffold: Plaid token exchange, webhooks, sync, AI orchestration |
| `project.yml` | [XcodeGen](https://github.com/yonaskolb/XcodeGen) project definition |
| `scripts/generate_icons.py` | Regenerates the app icons |

## Architecture

```
Features/  (SwiftUI views per surface: Home, Transactions, Receipts, Analytics,
            Budget, NetWorth, Accounts, Settings, Onboarding)
   │ renders state from
   ▼
Core/AI    (the engine: AIPipeline orchestrates categorization → recurring/transfer
            detection → receipt matching → SpendingProfile → SpendForecast →
            BudgetRiskAssessment → SafeToSpendDecision → narratives → widget snapshot)
   │ reads/writes
   ▼
Core/Models + Core/Persistence  (SwiftData store, App Group snapshot for widgets)
   ▲
Core/Services + Core/Networking (sync, Plaid Link, VisionKit receipt capture, auth)
```

Key principles:

- **Single source of truth** — every number on screen derives from normalized transactions/accounts via `AIPipeline.recompute`.
- **Structured AI** — model-backed stages return validated value types (`SpendingProfile`, `SafeToSpendDecision`, `Recommendation`, …). The UI never renders raw model output, and every recommendation carries evidence and a confidence score.
- **Safe-to-spend** is a deterministic budget computation × a bounded, always-explained behavioral adjustment (`Core/AI/SafeToSpendEngine.swift`). The explanation drawer shows every input.
- **No secrets on device** — Plaid access tokens and the Anthropic API key live only in the backend. The app holds a session token in the Keychain.

## Getting started (iOS app)

Requirements: macOS with Xcode 16+, [XcodeGen](https://github.com/yonaskolb/XcodeGen).

```bash
brew install xcodegen
xcodegen generate
open Spendrift.xcodeproj
```

Then build and run the `Spendrift` scheme. **The app runs fully in mock mode by default** (`AppEnvironment(useMocks: true)` in `SpendriftApp.swift`): it seeds six months of realistic accounts, transactions, receipts, and net-worth history, so every screen — including onboarding, receipt scanning UI, and widgets — is exercisable without a backend or Plaid credentials.

To run against a real backend, flip `useMocks` to `false` and point `BackendAPI.baseURL` at your deployment.

### TODOs before production

Search the codebase for `TODO(` — the important ones:

- `TODO(plaid-sdk)` — add the [Plaid Link iOS SDK](https://github.com/plaid/plaid-link-ios) via SPM and wire `PlaidLinkService`.
- `TODO(backend)` — Sign in with Apple token exchange and receipt upload.
- `TODO(production)` in `Backend/` — webhook verification, encrypted token storage, real job queue, object storage for receipts.
- Set `DEVELOPMENT_TEAM` in `project.yml`.

## Getting started (backend)

Requirements: Node 20+, PostgreSQL.

```bash
cd Backend
cp .env.example .env   # fill in Plaid + Anthropic credentials
createdb spendrift && psql spendrift < src/db/schema.sql
npm install
npm run dev            # http://localhost:3000
```

Endpoints: `POST /plaid/link-token`, `POST /plaid/exchange`, `POST /webhooks/plaid`, `GET /sync`, `POST /ai/classify`.

## Widgets

`SpendriftWidgets` ships Safe to Spend (small/medium/large + Lock Screen), Spend Pace, Budget Remaining ring, Net Worth, Upcoming Bills, and an AI Alert widget. All widgets read a precomputed snapshot from the App Group container (`SharedSnapshotStore`) — the app writes it after every sync/AI recomputation and reloads timelines, so widgets stay fresh without burning the WidgetKit refresh budget. Financial values are marked `privacySensitive()`.

## Icons

Minimal mark: an upward trend line resolving into a sparkle — the copilot reading your money's trajectory. Light, dark, and iOS 18 tinted variants are generated by:

```bash
pip3 install pillow
python3 scripts/generate_icons.py
```
